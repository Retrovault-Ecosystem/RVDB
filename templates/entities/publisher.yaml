# =====================================================
# RVDB Publisher Entity Template
# =====================================================

id: publisher.name

type: publisher

name: Publisher Name

aliases: []

relationships: {}

metadata: {}
